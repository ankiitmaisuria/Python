#Generate random numbers
"""Python doesn't have random() funtion to make random numbers, but Python has a buil-in module
called random that can generate random numbers"""

import random

print(random.randrange(1, 100))
